app:
  build: .
  command: puma
  ports:
    - "9292:9292"
  volumes:
    - ./srv:/srv


cron:
  build: .
#  volumes
  command: go-cron "*/10 * * * *" /bin/sh -c "rake scraper:scrape"
  volumes_from:
    - app
