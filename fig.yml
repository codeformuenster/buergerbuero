app:
  build: .
  command: puma
  ports:
    - "80:9292"
  volumes:
    - /srv

cron:
  build: .
  command: go-cron "*/10 * * * *" /bin/sh -c "ruby -r './scraper.rb' -e 'BuergerBuero::Scraper.new.save'"
  volumes_from:
    - app
